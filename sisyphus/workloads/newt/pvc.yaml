apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: newt-config-pvc
  namespace: newt
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-config
  resources:
    requests:
      storage: 1Gi
