apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: termix-data-pvc
  namespace: termix
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
