apiVersion: v1
kind: Namespace
metadata:
  name: omni-system
  labels:
    # Omni needs TUN + NET_ADMIN; required on clusters with Pod Security Admission.
    pod-security.kubernetes.io/enforce: privileged
